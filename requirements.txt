# Web server
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# Google generative models (Gemini / Veo)
google-genai>=0.3.0

# Azure OpenAI (gpt-image-1)
openai>=1.55

# Config & secrets
python-dotenv>=1.0
pydantic>=2.5

# Media fetching
httpx>=0.27

# Auth — user identification layer (backend/auth)
SQLAlchemy>=2.0          # ORM for the users table (sync engine, sqlite by default)
argon2-cffi>=23.1        # modern memory-hard password hashing (preferred over bcrypt)
PyJWT>=2.8               # signed JWT access + refresh tokens
email-validator>=2.1     # enables pydantic EmailStr validation
